<h1>All Contacts</h1>

<ul>
  <% $rolodex.contacts.each do |contact| %>
    <li>
      <a href="/contacts/<%= contact.id %>"><%= contact.first_name %> <%= contact.last_name %></a> Email: <%= contact.email %> Note: <%=contact.note%>
    </li>
  <% end %>
</ul>
